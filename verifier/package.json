{
  "name": "@olobolo-com/verifier",
  "version": "0.1.0",
  "description": "Verify an OLOBOLO evidence chain independently - no account, no network, no trust required.",
  "author": "OLOBOLO ApS",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "olobolo-verify": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "default": "./dist/lib.js"
    }
  },
  "files": ["dist", "src", "LICENSE", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "test": "tsc --noEmit -p tsconfig.test.json && bun test"
  },
  "dependencies": {
    "@olobolo-com/chain": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0"
  }
}
